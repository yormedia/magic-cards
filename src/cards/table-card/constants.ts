import { PREFIX_NAME } from "../../global/constants";

const _cardtype = `table`;

export const CARD_NAME = `${PREFIX_NAME}`.toLowerCase() + `-${_cardtype}-card`;
export const CARD_TITLE = `${PREFIX_NAME}` + `Card`;
export const CARD_DESCRIPTION = ``;
export const CARD_EDITOR_NAME = `${PREFIX_NAME}-${_cardtype}-editor`;
export const CARD_EDITOR_FILE = `./${PREFIX_NAME}-${_cardtype}-editor`;